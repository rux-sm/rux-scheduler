@AGENTS.md

`docs/status.md` is where this stands; `node tools/open.mjs` prints every open
item off the log rather than keeping a copy beside it. `docs/log.md` is the
record of every dated pass, newest first. `docs/rux-ds-requests.md` and
`docs/schema-requests.md` are what this app is waiting on from elsewhere; a gap
found here is a section added there, not a local workaround.
