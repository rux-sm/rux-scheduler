@AGENTS.md

`docs/log.md` is the record of every dated pass, newest first — read its top
entry for where the work stands. `docs/rux-ds-requests.md` and
`docs/schema-requests.md` are what this app is waiting on from elsewhere; a gap
found here is a section added there, not a local workaround.
