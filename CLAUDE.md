@AGENTS.md

`docs/status.md` is where this stands; `node tools/open.mjs` prints every open
item off the log rather than keeping a copy beside it. `docs/log.md` is the
record of every dated pass, newest first. A gap in rux-ds or in the schema is
fixed there, in the same session, never worked around here.
`docs/rux-ds-requests.md` and `docs/schema-requests.md` are the record of what
was asked and why; the live list of what is still open is the hub's
`docs/status.md`.
